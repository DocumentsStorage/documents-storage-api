version: "3.3"
services:
  documents-storage-api:
    container_name: documents-storage-api
    restart: always
    build:
      context: ../
      dockerfile: start.dockerfile
    hostname: documents-storage-api
    ports:
      - ${API_PORT}:${API_PORT}
    volumes:
      - ${API_DIR}:/usr/src/app/documents-storage-api/data
    networks:
      - documents_storage

  documents-storage-ui:
    container_name: documents-storage-ui
    restart: always
    build: ../../documents-storage-ui
    ports:
      - ${APP_PORT}:5000
  mongo-ds:
    container_name: documents-storage-db
    hostname: mongo-ds
    restart: always
    image: mongo
    volumes:
      - ${DB_DIR}:/data/db
    networks:
      - documents_storage

networks:
  documents_storage: